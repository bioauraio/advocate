# assets/

`letterhead_logo.png` — логотип для шапки фирменного бланка (положите свой,
рекомендуемая ширина ~600 px, PNG с прозрачным фоном). Используется скриптом
`tools/make_letterhead_doc.js` вместе с реквизитами из `tools/letterhead.json`.
